# RIFT DECK V55 - Status Theater / Command Tactics / Resume Safe

Version: 5.5.0
Deploy ID: RIFT-V550-STATUS-COMMAND-RESUME-20260915

## 1. 전투 종료 후 보상 화면 크래시 수정

- `renderReward()`에서 호출하던 `nextRerollCost()` 누락을 복구했습니다.
- 전투 승리 후 경험치 -> 보상 -> 상점/정비 -> 다음 조우 흐름이 중간 JavaScript 오류 때문에 멈추지 않도록 했습니다.
- READY 단계는 약 3.2초 뒤 자동 진행합니다.
- SHOP 단계도 조작이 없으면 약 8.5초 뒤 자동 진행합니다. 버튼은 빠른 진행용으로 남겨두었습니다.

## 2. 이어하기 강화

- 홈의 이어하기는 현재 메모리에 방이 없어도 `profile.activeRoomId`가 있으면 표시됩니다.
- `/api/rooms/resume`을 통해 서버의 활성 원정을 다시 찾습니다.
- V55는 프로필에 `activeRoomSnapshot` 폴백을 함께 저장합니다.
- 선택적 `rift_rooms` 테이블이 없거나 서버가 재배포되어 메모리 방이 사라진 경우에도 프로필 스냅샷에서 복구할 수 있습니다.
- 종료/클리어된 원정의 오래된 이어하기 포인터와 스냅샷은 정리됩니다.

## 3. 상태이상 연출 추가

전투 중 아래 효과는 배지 텍스트뿐 아니라 실제 화면 연출이 발생합니다.

- 화상
- 중독
- 감전
- 공격 감소
- 방어 감소
- 행동 봉쇄
- BREAK / 자세 붕괴
- 보호막
- 회복
- 강화

상태이상이 새로 적용되면 대상 몬스터 위에서 전용 글리프, 링, 파티클, 상태명 팝업이 재생됩니다. 화상/중독 지속 피해도 별도 tick 연출이 재생됩니다.

## 4. 기술 효과 다양화

- 기존 V54의 몬스터별 전용기 205종을 유지합니다.
- 기술 연출 계열은 베기, 발톱, 물기, 돌진, 광선, 탄환, 번개, 화염, 얼음, 파동, 회오리, 뿌리, 포자, 독, 그림자, 운석, 지진, 시간, 음악, 거울, 회복, 방어 등으로 분리되어 있습니다.
- 같은 계열도 기술/몬스터별 variant, 타격 수, 속성, 시그니처 연출을 조합하여 같은 애니메이션의 색만 바꾸는 느낌을 줄였습니다.
- Windows/브라우저의 `prefers-reduced-motion` 설정이 켜져 있다는 이유만으로 전투 FX 전체가 사라지던 문제를 수정했습니다. 게임 설정의 `전투 이펙트 OFF`를 직접 선택한 경우에만 FX가 꺼집니다.

## 5. 중독 및 디버프 전투 로직

- 독 계열 기술과 일부 종 전용기에 `중독`을 추가했습니다.
- 중독은 적 턴 시작 시 지속 피해를 주고 스택이 감소합니다.
- 실제 적용된 상태가 전투 이벤트에 포함되어 클라이언트의 상태 연출과 동기화됩니다.
- `약화/취약` 표시는 각각 `공격↓/방어↓`로 바꾸어 의미를 바로 알 수 있게 했습니다.

## 6. 스펠 UI를 `공명 지령` 시스템으로 재정리

기존 스펠은 전투의 주인공인 몬스터 기술과 경쟁하는 별도 공격 수단처럼 보였기 때문에 V55에서는 UI 정체성을 `공명 지령`으로 통일했습니다.

- 전투 메뉴: `공명 지령`
- 설명: `즉시 지원 + 다음 기술 강화`
- 같은 속성 지령: 다음 기술 +20%, BREAK +6
- 다른 속성 지령: 다음 기술 +10%, BREAK +3
- 카드 자체의 즉시 효과(회복, 보호막, 화상, 중독, 감전, 공격 감소, 방어 감소 등)를 한 줄로 표시
- 지령 사용 시 전용 배너/봉인 문양/상태 연출이 재생됩니다.
- 몬스터의 행동 횟수는 소비하지 않습니다.

내부 데이터 타입은 저장 호환성을 위해 `spell`을 유지하지만, 사용자에게 보이는 주요 명칭은 `공명 지령`으로 정리했습니다.

## 7. 기술 선택 UI 겹침/잘림 개선

- 기술 패널 고정 높이 때문에 2x2 기술 카드가 잘리던 구조를 제거했습니다.
- 기술 카드 최소 높이를 늘리고 command panel에 실제 스크롤을 허용했습니다.
- 기술명, 속성, 공격 유형, 위력, 명중률, 재사용 대기, 상태 효과 태그를 크게 표시합니다.
- 모바일에서는 기술을 1열로 배치하여 글자와 상태 태그가 서로 겹치지 않습니다.

## 8. 원정 보너스 화면 가독성 개선

- 원정 보너스 제목, 설명, 선택지 이름과 상세 효과의 글자 크기와 대비를 크게 높였습니다.
- `최대 HP +12 / 시작 방어 +6 / 시작 골드 -40` 같은 전체 효과 문구를 잘라내지 않고 표시합니다.
- 모바일에서는 3개 선택지를 세로로 전환합니다.

## 9. 테스트

`npm test` 전체 통과:

- 50층 완주
- 4인 멀티
- 전투/더블 전투
- 봉인
- 진화/변형폼 615종
- 장착 아이템
- 로그인/클라우드 룸
- V53/V53.1/V53.2 회귀 테스트
- V54 전용기/전투 페이싱
- V55 상태 연출/공명 지령/서버 재시작 후 이어하기

V55 전용 테스트 결과:

`V55_STATUS_COMMAND_RESUME_OK signatures=205 fxFamilies=19 poisonSigs=13 resume=<roomId>`

## 배포 확인

Render 재배포 후 `/healthz`에서 다음 값을 확인하세요.

- `version`: `5.5.0`
- `deployId`: `RIFT-V550-STATUS-COMMAND-RESUME-20260915`

그 뒤 브라우저에서 Ctrl+Shift+R 강력 새로고침을 권장합니다.
