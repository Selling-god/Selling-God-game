# FUSEWILD v6.0 패치 적용

1. 기존 프로젝트를 백업합니다.
2. 이 패치 ZIP 안의 파일/폴더를 **프로젝트 루트에 그대로 덮어쓰기** 합니다.
3. `npm install` 후 `npm test`를 실행합니다.
4. Render/GitHub Pages가 아니라 Node Web Service 방식으로 배포 중이라면 서버를 재시작합니다.
5. 브라우저에서 Ctrl+F5로 강력 새로고침합니다.

## 핵심
- 게임명: **FUSEWILD / 퓨즈와일드**
- 자동 웨이브 전환을 느리게 조정하고, 실제 적이 화면에 나온 뒤 실제 적 이름으로 조우 문구 표시
- 전투 후 **상점 → 무료 보상 1개 선택 → 다음 웨이브** 순서
- 융합: 5턴 유지, 전투 종료 시 즉시 해제, 원래 두 몬스터 복원
- 융합 이미지/프레임 자산: `assets/fusion/` 포함
