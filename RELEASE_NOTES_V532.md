# RIFT DECK V53.2

- 전투 종료 시 서버 상태를 즉시 커밋하고 최대 1.35초 내 보상 화면으로 전환합니다. 1.8초 강제 복구 타이머를 추가해 빈 전투 화면 고착을 방지합니다.
- 각 몬스터/기술 조합에 speciesId + moveId 기반 고유 시그니처 이펙트를 추가했습니다. 속성 색, 문양, 입자 패턴, 링 형태가 몬스터별로 달라집니다.
- 이어하기를 브라우저 localStorage 전용에서 서버 프로필 activeRoomId + Supabase 방 스냅샷 기반으로 확장했습니다. 같은 브라우저뿐 아니라 로그인 계정의 다른 기기에서도 활성 원정을 복구할 수 있습니다.
- 로그인 시 roomId를 지운 뒤 원정을 잃던 문제를 수정했습니다.
- 새 API: POST /api/rooms/resume
- 배포 ID: RIFT-V532-BATTLEFLOW-RESUME-FX-20260915
- 이어하기 복구 시 저장된 roomId가 현재 로그인 계정의 방인지 다시 검증하고, 불일치/만료 시 서버의 활성 원정 조회로 자동 폴백합니다.
