# FUSEWILD v6.2.1 — LIVE SYNC / REAL FUSION ART / LARGE TYPE

## 융합 이미지
- SVG 안에서 외부 이미지를 다시 불러오던 방식을 제거했습니다. 이 방식은 브라우저의 SVG 이미지 보안 제약 때문에 실제 몬스터가 빈 카드처럼 보일 수 있었습니다.
- 서버가 두 원본 몬스터 PNG를 직접 디코딩하고 대각선 마스크·투명도·속성 광륜을 합성해 `assets/fusion/generated/*.png`에 실제 PNG를 생성합니다.
- 생성물은 320x320 투명 PNG이며 같은 조합은 캐시를 재사용합니다.
- 5턴 카운트 및 전투 종료 시 즉시 해제 규칙은 유지합니다.

## 전투 실시간 동기화
- 기술 설명과 실제 HP 변화 사이의 지연을 줄였습니다. 서버 이벤트에 공격 전/후 HP 스냅샷을 포함하고, 기술 이펙트가 닿는 시점에 HP 바/숫자가 애니메이션됩니다.
- 공격자 자신의 회복/흡혈/반동 HP도 같은 방식으로 반영합니다.
- 적의 회복 HP, 독/화상 틱 피해, 상태이상 적용/해제, 융합 5T 카운트와 해제 이미지를 narration과 같은 타임라인에서 갱신합니다.
- 서버의 최종 상태는 먼저 받아도, 전투 화면은 이벤트 타임라인을 끝낼 때까지 조작 잠금 후 실시간 단계별 시각 상태를 유지합니다.

## 큰 글씨
- 데스크톱 전투 이름 20px, HP 15px, 설명 메시지 26~32px, 루트 명령 21px, 기술명 19px 수준으로 확대했습니다.
- 기술 속성/PP/효과 태그도 10~12px로 올렸습니다.

## 전투 후 화면
- 전체 화면을 덮는 거대한 패널 대신 전장을 위에 남기고 하단 약 470~520px에 상점/무료 보상/파티/메시지를 밀도 있게 배치했습니다.
- 상점 6칸 + 무료 보상 3칸 + 6마리 파티 상태를 한 화면에 표시하며 데스크톱에서 내부/브라우저 이중 스크롤이 생기지 않도록 했습니다.

## QA
- `npm test`: BUILD_OK + V62_STATIC_OK + V62_RUNTIME_OK
- 205종 몬스터 카탈로그, 실제 융합 PNG 생성, 전투 HP 스냅샷, 5턴 자동 분리 검증.
