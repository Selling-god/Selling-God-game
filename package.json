{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "6.0.0",
  "private": true,
  "description": "FUSEWILD: original monster fusion roguelite with readable wave pacing, post-battle shop/reward cadence, persistent HP/PP/status, 10-wave biome progression, five-turn fusion, capture, co-op, and resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v60-cadence-brand-test.js --static",
    "test": "npm run build && node scripts/v60-cadence-brand-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
