{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "6.2.1",
  "private": true,
  "description": "FUSEWILD: original monster fusion roguelite with compact PokeRogue-inspired battle/shop pacing, readable large-type UI, staged real-time battle visuals, persistent HP/PP/status, five-turn fusion, capture, co-op, and resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v62-quality-test.js --static",
    "test": "npm run build && node scripts/v62-quality-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
