{
  "name": "rift-deck-abyss-expedition",
  "version": "5.7.0",
  "private": true,
  "description": "Original pixel monster roguelite with serialized battle message phases, cinematic move/status FX, persistent HP/PP/status, 10-wave recovery, turn-cost fusion, signature moves, held items, capture, co-op, and resilient resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v57-battle-phase-fx-test.js --static",
    "test": "npm run build && node scripts/v57-battle-phase-fx-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
