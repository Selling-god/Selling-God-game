{
  "name": "rift-deck-abyss-expedition",
  "version": "5.6.0",
  "private": true,
  "description": "Original pixel monster roguelite with persistent HP/PP/status, 10-wave recovery, turn-cost fusion, 205 signature moves, distinct move/status FX, held items, capture, co-op, and resilient resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node scripts/v56-fusion-pp-battle-test.js --static",
    "test": "npm run build && node scripts/v56-fusion-pp-battle-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
