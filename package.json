{
  "name": "rift-deck-abyss-expedition",
  "version": "2.2.1",
  "private": true,
  "description": "Dynamic server-authoritative 1-4 player card roguelite with Supabase cloud accounts, persistent progression, and impact-heavy combat VFX.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node scripts/catalog-test.js",
    "test": "npm run build && node scripts/smoke-test.js && node scripts/multiplayer-test.js && node scripts/combat-test.js && node scripts/fifty-floor-test.js && node scripts/auth-test.js && node scripts/cloud-room-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
