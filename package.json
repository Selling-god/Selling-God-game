{
  "name": "rift-deck-abyss-expedition",
  "version": "2.5.0",
  "private": true,
  "description": "Dynamic 1-4 player pixel card roguelite with run contracts, relic drafts, camp upgrades, reward salvage, abyss pressure, and server-authoritative combat.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node scripts/catalog-test.js",
    "test": "npm run build && node scripts/smoke-test.js && node scripts/multiplayer-test.js && node scripts/combat-test.js && node scripts/combat-depth-test.js && node scripts/fifty-floor-test.js && node scripts/auth-test.js && node scripts/cloud-room-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
