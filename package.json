{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "8.0.0",
  "private": true,
  "description": "FUSEWILD: Element Relay, turn fusion, original monster roguelite. Expedition UI and accessibility release.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check public/resonance.js && node --check data/move-library.js",
    "test": "npm run build && node scripts/run-release-tests.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
