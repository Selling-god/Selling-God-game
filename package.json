{
  "name": "rift-deck-abyss-expedition",
  "version": "1.0.0",
  "private": true,
  "description": "Original dynamic-server browser card roguelite with 1-4 player co-op dungeons.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node scripts/smoke-test.js && node scripts/multiplayer-test.js && node scripts/combat-test.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
