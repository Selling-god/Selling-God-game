{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "6.3.0",
  "private": true,
  "description": "FUSEWILD v6.3: original monster fusion roguelite with phase-synchronized battle narration, manual faint replacement, mobile-safe battle/reward UI, five-turn fusion, capture, co-op, and resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v63-quality-test.js --static",
    "test": "npm run build && node scripts/v63-quality-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
