{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "6.1.0",
  "private": true,
  "description": "FUSEWILD: original monster fusion roguelite with PokeRogue-inspired compact battle/shop cadence, synchronized battle messages, persistent HP/PP/status, five-turn fusion, capture, co-op, and resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v61-ui-cadence-test.js --static",
    "test": "npm run build && node scripts/v61-ui-cadence-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
