{
  "name": "rift-deck-abyss-expedition",
  "version": "5.9.0",
  "private": true,
  "description": "Original pixel monster roguelite with compact wave rewards, snappy battle FX, and temporary five-turn fusion that always releases at battle end.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js && node scripts/v59-fusion-layout-perf-test.js --static",
    "test": "npm run build && node scripts/v59-fusion-layout-perf-test.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}
