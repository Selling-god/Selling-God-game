{
  "name": "fusewild-monster-fusion-roguelite",
  "version": "6.4.0",
  "private": true,
  "description": "FUSEWILD: original monster fusion roguelite. PokeRogue-style cadence (shop after every wave, wave-10 NPC keeper duels), mode-specific heal rules, five-turn fusion, capture, co-op and resume.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check data/move-library.js",
    "test": "npm run build && node scripts/run-all-tests.js"
  },
  "engines": {
    "node": ">=20 <27"
  }
}