/** Legacy compatibility file. RIFT DECK is served by server.js.
 * Do not use Next static export for production; Render must run npm start. */
const nextConfig = { poweredByHeader:false, images:{unoptimized:true} };
export default nextConfig;
