# FUSEWILD v6.0 — PokéRogue-style Cadence / Fusion Lifetime Patch

## 이름 / 브랜딩
- 표시 이름을 **FUSEWILD (퓨즈와일드)** 로 변경했습니다.
- 부제는 **MONSTER FUSION ROGUELITE** 입니다.
- 기존 세이브 호환을 위해 내부 `riftdeck.*` localStorage 키는 변경하지 않았습니다.

## 진행 속도 / 조우 흐름
- 일반 단일 경로 웨이브는 별도 조우 버튼을 반드시 누르지 않아도 자동으로 전투에 진입합니다.
- 전투 전 지역 전환은 일반 3.6초, 특수 조우 4.4초, 보스 5.2초로 늘렸습니다.
- 전환 화면은 실제 배경 지역 정보만 안내하고, **적 이름/실제 조우 문구는 적 스프라이트가 화면에 등장한 뒤** 표시하도록 바꿨습니다.
- 원하면 `바로 전투 시작`으로 대기 시간을 건너뛸 수 있습니다.
- 전투 내 한국어 텍스트 표시/유지 간격과 주요 기술 연출 간격을 늘려 글과 화면 연출이 엇갈리는 현상을 줄였습니다.

## 전투 후 흐름
- 일반 승리 후 **상점/정비 → 무료 보상 1개 선택 → 확인 → 다음 웨이브** 순서로 정리했습니다.
- 무료 보상을 고르기 전에는 상점 구매, 정비, 재굴림을 계속 사용할 수 있습니다.
- 보상 선택 후 약 2.1초 결과 확인 시간을 두고 다음 웨이브로 넘어갑니다.
- 다음 웨이브의 지역 전환 화면까지 이어져 보상 화면이 갑자기 사라지는 느낌을 줄였습니다.

## 융합
- 융합은 행동 1회를 소비해 발동합니다.
- 발동 직후부터 **다음 5번의 전투 턴** 동안 유지되며, 5턴이 끝나면 자동 해제됩니다.
- 전투가 먼저 끝나면 남은 턴과 관계없이 즉시 해제됩니다.
- 해제 시 원래 두 몬스터가 파티/대기 위치로 복원됩니다.
- 융합 중 사용한 공통 기술의 PP/숙련 정보는 원래 몬스터 기술에 반영됩니다.
- 모든 조합에 대해 결정적인 조합 이름을 만들고, 두 몬스터 스프라이트를 사용한 조합별 런타임 융합 이미지를 표시합니다.
- `assets/fusion/`에 융합 프레임/그리드/시길 원본 자산을 포함했습니다.

## 성능
- Canvas DPR, 파티클 수, 일반 기술 FX 지속시간을 낮춰 GPU 중첩을 줄였습니다.
- 텍스트를 느리게 만들면서도 기술 이펙트 자체가 과도하게 길어져 렉이 생기지 않도록 분리 조정했습니다.

## 검증
- `npm run check` 통과
- `npm test` 통과
- 런타임 테스트에서 융합 `5T → 4T → 3T → 2T → 1T → 해제`, 원래 두 몬스터 복원, 전투 종료 후 runState 복원을 검증했습니다.
