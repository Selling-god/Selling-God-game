# FUSEWILD V8 · EXPEDITION — 패치 적용 안내

버전: 8.0.0  
배포 식별자: `FUSEWILD-V800-EXPEDITION-20260921`  
기준 원본: 이번 대화에 첨부한 `Selling-God-game-main (5)(2).zip`

## 이 파일은 전체 게임이 아닌 패치입니다

원본 프로젝트에 교체·추가할 파일만 들어 있습니다. 기존 `assets/`, `data/` 및 변경하지 않은 파일은 원본을 그대로 사용합니다. 패치 ZIP만 따로 실행하면 게임이 동작하지 않습니다. 다른 버전의 프로젝트에 무조건 덮어쓰지 마세요.

## 적용 순서

1. 실행 중인 서버를 멈추고 원본 프로젝트를 별도 폴더에 백업합니다. 로컬 저장을 사용한다면 `data/profiles.json` 또는 `PROFILE_FILE` 환경변수로 지정한 파일도 별도로 백업하세요. 기존 `.env`와 클라우드 설정은 유지합니다.
2. 패치 ZIP을 풉니다. 안에 있는 `server.js`, `package.json`, `public/`, `scripts/` 등을 **원본의 `package.json`이 있는 폴더**에 복사하고 같은 이름의 파일을 덮어씁니다. 폴더를 한 단계 더 만들어 넣는 방식이 아닙니다.
3. 그 폴더에서 터미널을 열고 아래 명령을 실행합니다.

```sh
npm run check
npm test
npm start
```

서버 기본 포트는 3000입니다. 브라우저에서 `http://localhost:3000`에 접속합니다. `PORT`를 별도로 지정했다면 그 포트로 접속하세요.

기존 프로젝트의 Node 지원 범위는 `>=20 <27`입니다. 이번 변경으로 새 npm 런타임 의존성을 추가하지 않았습니다. 선택 사항인 화면 테스트에만 Python Playwright가 필요합니다.

## Render 등에 배포 중이라면

기존 Node Web Service의 저장소에 패치 파일을 반영한 다음 다시 배포하세요. 기존 환경변수와 저장소 설정은 유지합니다. `npm start`로 Node 서버가 실행되어야 합니다.

`out/index.html`은 정적 호스팅 오배포를 알려 주는 **진단 페이지**입니다. `out/`만 업로드하거나 `public/index.html`을 더블클릭하는 방법으로는 API·저장·협동 게임이 실행되지 않습니다.

배포 후 `/healthz`에서 `version: 8.0.0`과 위 배포 식별자를 확인하세요. 예전 화면이 남으면 Ctrl+Shift+R로 새로고침합니다. **게스트 저장 식별자가 사라질 수 있으므로 브라우저 사이트 데이터 전체를 지우지 마세요.**

## 실제 변경 사항

### 화면과 조작

홈, 파티 편성, 전투, 전투 후 보상 화면을 다시 구성했습니다. 기존 몬스터 그림을 유지하면서 이미지 영역을 고정하고, 작은 화면에서는 세로로 배치하고 페이지를 스크롤하도록 했습니다. 화면 전체를 강제로 축소해 글자와 몬스터를 작게 만드는 방식은 사용하지 않았습니다.

파티 편성에는 이름·속성·등급 검색, 6슬롯과 포인트 예산 표시, 속성 구성 안내가 있습니다. 전투에서는 기술명, 명중률, PP, 상성, BREAK 진행도를 확인할 수 있습니다. 설정에는 전투 속도, 효과음, 이펙트, 움직임 줄이기, 큰 글씨가 있습니다.

### 속성 연계 — 기존 융합과 함께 사용하는 새 규칙

서로 다른 속성의 공격·버스트 기술을 3번 명중시키면 세 번째 기술의 **기본 위력과 공격력 계수에 20% 증가**, BREAK 수치에 8이 추가됩니다. 이것은 최종 HP 피해가 항상 정확히 20% 증가한다는 뜻은 아닙니다. 상성·방어막·추가 공격 보너스·반올림 등 기존 계산이 뒤에 적용됩니다.

진행 중인 속성을 다시 사용하면 해당 속성 1개부터 다시 시작합니다. 방어·회복·지원기, 빗나간 공격은 진행도를 유지합니다. 교대와 융합 후에도 유지되며, 전투가 바뀌면 초기화됩니다. 협동 플레이에서는 플레이어별로 따로 관리됩니다.

### 실제 기술과 연결된 상대 행동 예고

이전의 임의적인 예고값 대신 서버가 상대의 다음 기술을 미리 선택하고 그 기술명을 표시합니다. 상대 행동 때 같은 기술을 실행하며, 예약된 기술은 로컬 저장·서버 재시작 복원에도 유지됩니다. BREAK·행동 봉쇄·상태이상·전투 종료 등에 따라 예정 행동은 취소될 수 있습니다. 보스의 단계 전환 시에는 계획이 다시 정해집니다.

공격 기준값은 최종 피해 예측이 아닙니다. 방어 기술은 방어막 수치, 회복 기술은 회복량으로 구분해 표시합니다. 다음 기술 공개에 따른 난이도 변화는 장기 플레이테스트가 추가로 필요합니다.

### 보상과 안정성

무료 보상 선택 → 장착 대상 지정 → 선택적 상점 이용 → 직접 다음 웨이브 진행 순서입니다. 보상 장착·분해 후 남아 있던 시간제 자동 진행을 제거했습니다.

전투 연출이 끝나는 동안 서버가 이미 보상 화면으로 넘어갔는데, 늦게 끝난 연출이 옛 전투 화면을 다시 덮는 오류를 수정했습니다. 진행 중인 방 변경 요청의 중복 실행을 막고, 오래된 순번의 상태 업데이트를 무시하며, API 대기 제한과 연결 안내를 추가했습니다. 통신 실패 시 구매·기술 요청을 임의로 자동 재전송하지 않습니다.

## 유지한 부분

몬스터 205종과 기존 그림, 아이템 144종의 카탈로그, 기존 기술 데이터, 경험치·기술 습득 간격, 5턴 융합, 전투 종료 시 융합 해제, 여행/던전 회복 구분, 50층 던전, 상점 가격, 소환 가격·확률을 유지했습니다. 새로운 기술 수백 개나 몬스터 그림을 추가했다고 주장하는 업데이트는 아닙니다.

기존 원정 저장을 읽도록 처리했고, 연계 필드가 없는 로컬 저장도 검증했습니다. 실제 운영 중인 모든 클라우드 저장의 호환성을 확인한 것은 아니므로 적용 전 백업은 필요합니다.

## 파일과 검증 자료

- `PATCH_MANIFEST_V800.json`: 교체·추가 파일, 원본/변경 SHA-256, 원본 ZIP 해시.
- `QA_REPORT_V800_EXPEDITION.md`: 수행한 테스트, 결과와 검증하지 않은 환경.
- `scripts/v8-element-relay-test.js`: 브라우저/Node 공용 연계 규칙 검사.
- `scripts/v8-runtime-relay-test.js`: 실제 로컬 HTTP 전투·저장·상대 기술 예약 검사.
- `scripts/qa/`: 선택 사항인 Chromium 화면 검사.

## 되돌리기

서버를 멈춘 후 백업한 프로젝트와 해당 시점의 저장 파일을 함께 복구합니다. 패치 적용 뒤 새로 진행한 게임 데이터까지 과거 백업에 자동으로 합쳐 주는 도구는 포함하지 않았습니다.

이번 패치에는 계정, 비밀번호, 서비스 키, 사용자의 저장 파일, 폰트 파일, 테스트용 재화가 포함되지 않습니다.
