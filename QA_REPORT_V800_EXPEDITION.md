# FUSEWILD V8 · EXPEDITION — QA 보고서

검증일: 2026-09-21  
버전: 8.0.0 / `FUSEWILD-V800-EXPEDITION-20260921`  
기준: 사용자가 첨부한 ZIP에 실제 적용한 로컬 변경분

## 최종 확인 결과

| 검사 | 결과 | 범위 |
|---|---|---|
| `npm run check` | 통과 | 서버, 클라이언트, 공용 연계 모듈, 기술 라이브러리 문법 |
| `npm test` | **26/26 테스트 묶음 통과** | 빌드·카탈로그·기존 회귀 검사 24개 + 신규 2개 |
| 원본 ZIP에 패치 새로 적용 후 재검사 | **문법 및 26/26 테스트 묶음 통과** | 별도 폴더에 원본을 풀고 패치 파일만 덮어씌워 검증 |
| 공용 속성 연계 검사 | **65개 확인 통과** | Node 및 브라우저 UMD, 입력 불변성, 초기화, 연계·중복·실패·지원기 |
| V8 HTTP 런타임 검사 | **44개 확인 통과** | 실제 로컬 서버, 기술·PP·교대·저장 복원·연계 보너스·다음 전투 초기화·적 기술 예약 |
| Chromium 화면·조작 검사 | **49개 화면/해상도 조합, 410개 확인 통과** | 7개 화면 × 7개 뷰포트와 실제 버튼/API 조작 |
| 해당 Chromium 검사 중 포착한 미처리 클라이언트 예외 | **0건** | 테스트한 시나리오 범위에 한함 |

입력 원본에서는 기존 회귀 검사 24/24가 통과했습니다. 이 결과는 기존 게임이 완성품이었다는 뜻이 아니라, 기존 자동 검사 기준을 만족했다는 뜻입니다. V8에서도 그 동작 검사를 유지하고 새로운 검사를 추가했습니다.

화면 검사의 세부 확인 개수는 무작위로 생성되는 적·보상에 따라 달라질 수 있습니다. 위 410개는 마지막 성공 실행에서 기록된 수입니다. `scripts/qa/v8_browser_test.py`가 JSON 결과를 기록합니다.

## 검사한 화면과 크기

화면: 홈 / 파티 편성 / 싱글 전투 루트 / 기술 선택 / 더블 전투 레이아웃 / 전투 후 보상 / 소환.

뷰포트: 320×740, 390×844, 768×1024, 844×390, 1366×768, 1440×960, 1920×1080.

가로 넘침, 이미지 로드, 이미지 요소가 부모 영역에 들어오는지, 명령 패널이 전장을 덮지 않는지 등을 확인했습니다. 더블 전투의 일부 화면은 배치 검사를 위해 복제한 적을 사용하는 테스트용 상태입니다. 특정 기기의 실제 성능을 인증한 것은 아닙니다.

추가 조작: 가이드 열기/닫기, 모달 접근성·포커스 순환·배경 비활성화, ESC, 모달 중 숫자 전투 단축키 차단, 파티 검색/추가/제거/저장, 로컬 서버에 전달되는 기술 사용, 기존 전투 연출, 설정 변경, 오래된 상태 거부, 장착 보상 대상 선택, 5.2초 뒤에도 보상 화면 유지, 명시적 다음 진행, 테스트 계정의 소환 재화 1회 차감.

## 실제로 찾아 수정한 문제

1. 이전 CSS의 높이·넘침·변형 규칙이 새 화면에 남아 몬스터와 패널이 겹치는 부분을 분리하고, 콘텐츠 높이를 기준으로 배치했습니다.
2. 상단과 하단 위치가 동시에 지정된 저장 알림이 모바일에서 화면을 길게 덮던 문제를 수정했습니다. 알림은 콘텐츠 높이만 사용하고 전투 버튼의 터치를 가로채지 않습니다.
3. 전투 연출을 기다린 뒤 오래된 전투 상태를 다시 렌더링해 보상 화면이 되돌아가는 경합을 수정했습니다. 늦게 완료된 작업은 현재 방·화면·상태를 다시 확인합니다.
4. 장착 후 5.2초, 분해 후 2.1초에 남아 있던 자동 진행을 제거했습니다.
5. 기존 상대 행동 예고가 실제 AI 기술 선택과 연결되지 않았습니다. 예약된 기술을 저장하고 실제 적 턴에서도 같은 기술을 선택하도록 변경했습니다. 로컬 서버 재시작 뒤 예약이 유지되는 것도 검사했습니다.

## 신규 전투 검사의 범위

연계 규칙은 같은 함수를 브라우저와 서버에서 사용합니다. 세 번째 고유 속성의 적중, 중복 속성 재시작, 빗나감·지원기의 보존, 플레이어별 분리, 구형 저장 필드 누락을 검사했습니다.

실제 서버에서 공격·교대·공격·교대·공격을 수행하고 PP가 한 번만 소모되는지, 연계 보너스가 이벤트와 전투 계산에 반영되는지 확인했습니다. 동일 저장의 연계 유무를 바꾼 대응 비교로 실제 피해 증가를 검사했습니다. 최종 피해는 방어막 등으로 인해 정확히 1.20배가 아닐 수 있습니다.

적의 예정 기술이 실제 실행 기술과 일치하는지, 다음 라운드의 계획 갱신, 계획의 서버 재시작 복원, 구형 저장의 예고 필드 보완도 검사했습니다.

## 기존 테스트 수정에 관하여

기존 테스트의 버전·CSS 클래스·표시 문구 확인을 V8에 맞게 조정했습니다. 기존 런타임 검사를 삭제하거나 실패를 무시하는 방식으로 통과시키지 않았습니다.

두 가지 무작위 테스트 조건을 안정화했습니다. 기존 융합 PP 검사는 적의 무작위 수면·빙결에 의해 행동 자체가 취소될 수 있어, 기존 TEST_MODE 전용 도구로 상태를 제거한 뒤 실제 기술의 PP를 검사하도록 했습니다. 신규 연계 배율 검사는 무작위 방어막의 고정 차감이 배율 비교를 왜곡하지 않도록 약한 기본 공격만 하는 테스트 적을 사용했습니다. 상태이상과 방어막의 실제 게임 규칙은 이 이유로 바꾸지 않았습니다.

## 재현 방법

```sh
npm run check
npm test
node scripts/v8-element-relay-test.js
node scripts/v8-runtime-relay-test.js
```

선택 사항인 화면 검사에는 Python Playwright와 호환 Chromium이 필요합니다. 이미 설치된 환경에서는 다음과 같이 실행합니다.

```sh
python scripts/qa/v8_browser_test.py --output qa-v8-output
```

시스템 Chromium을 명시할 때는 `--chromium /경로/Chromium` 옵션을 사용합니다. 이 실행 환경에서는 `/usr/bin/chromium`을 사용했습니다. QA 서버와 임시 프로필은 별도로 생성하며 실제 운영 계정을 사용하지 않습니다.

## 검증 환경의 한계

Chromium 화면 검사는 HTML/CSS/JS를 메모리에 로드하고 실제 로컬 Node HTTP API와 연결하는 하네스를 사용했습니다. 로컬 페이지 직접 탐색에 대한 브라우저 환경 제약 때문에 택한 방식입니다. **EventSource와 localStorage는 테스트 대역**이므로 실제 배포 URL 탐색, 실제 SSE 재연결, 브라우저 재시작 뒤 저장 지속성까지 검증했다는 뜻이 아닙니다. 테스트 대역이 실제 앱 코드에 설치되는 것은 아닙니다.

기존 auth/cloud/multiplayer 자동 검사가 통과했지만, 실제 Supabase 프로젝트·로그인 메일·운영 중인 클라우드 데이터·외부 네트워크 협동 세션에 접속해 검증한 것은 아닙니다.

빌드에는 원본부터 있던 선택적 Supabase SQL 설정 파일 누락 경고가 있습니다. 빌드는 정상 종료되며 이번 패치에서 데이터베이스 구조를 변경하지 않았습니다. 운영 클라우드 설정의 완전성은 별도 확인이 필요합니다.

## 상용 공개 전에 남은 검증

이번 결과는 실행 가능한 개선판과 자동 검사 결과이지, 포켓로그를 능가한다는 비교 검증이나 상용 출시 인증이 아닙니다.

- 실제 iPhone Safari, Android Chrome, 태블릿, 저사양 PC에서 터치·성능·발열·장시간 실행.
- 실제 2~4인 외부 네트워크 플레이, 끊김·동시 입력·재접속·서버 재시작과 클라우드 복구.
- 장기 플레이테스트: 속성 연계, 공개된 적 기술, 반복 플레이의 다양성, 후반 골드와 난이도, 이탈 지점.
- 기존 원본에 들어 있는 모든 그림·음원·폰트·명칭 및 기타 자산의 출처·이용권한 확인. 이번 패치의 신규 코드에는 포켓로그 소스나 외부 게임 그림을 가져오지 않았습니다.
- 공개 서비스의 인증·접근 제어·데이터 백업·복원·운영 보안 검토. 결제나 유료 상점은 이번에 추가하거나 검증하지 않았습니다.

운영 데이터와 적용 전 소스는 반드시 백업한 뒤 배포하세요.
