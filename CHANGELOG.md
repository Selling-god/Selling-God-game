# v6.3.0 — TRAINER DUEL / EVERY-WAVE SHOP / MODE HEAL RULES

## 게임플레이

**상점이 매 웨이브 열립니다.** 기존에는 10웨이브 중 4·9웨이브에만 열렸습니다
(`shopWave=[4,9]`). 직접 정리하신 `docs/POKEROGUE_FLOW_RESEARCH_V62.md`에는
"대부분의 전투 뒤 상점이 먼저 열린다"고 적혀 있었는데 구현이 따라가지 않았습니다.
이제 모든 전투 웨이브에서 `전투 → 상점 → 무료 보상 1개 → 다음 웨이브` 리듬이 돕니다.
재고는 웨이브 비중에 따라 차등 배분됩니다.

| 웨이브 | 재고 |
|---|---|
| 일반 | 3 |
| 7 (균열 추적자) | 4 |
| 5 (균열 워든) | 5 |
| 10 (관리자 결투) | 6 |

50층 골드 시뮬레이션 결과, 매 웨이브 구매해도 런이 유지됩니다. 초반은 빠듯하고
후반은 여유가 생깁니다 (후반 골드 과잉은 이전부터 있던 밸런스 사안입니다).

**10웨이브가 NPC 관리자 결투로 바뀌었습니다.** 얼굴 없는 보스 1체 대신,
이름·칭호·등장/패배 대사를 가진 바이옴 관리자가 2체 편성으로 승부를 겁니다.
에이스는 기존 바이옴 보스(HP 분할 유지), 파트너는 같은 바이옴의 상위 야생입니다.
보스 봉인 규칙(로열 봉인구 + HP 25% 이하)은 그대로입니다.

**회복 규칙이 모드별로 갈립니다.**
- 여행: 10웨이브 관리자 격파 후 파티 완전 회복 (기존과 동일)
- 던전: **자동 회복 없음.** 50층 내내 HP·PP·상태가 누적되며 상점 정비와
  회복 아이템으로만 회복합니다.

도망(RUN) 커맨드는 추가하지 않았습니다.

## 저장소 정리

**다른 프로젝트가 섞여 있었습니다.** `app/`, `lib/`, `next.config.mjs`,
`tsconfig.json`, `next-env.d.ts`와 문서 약 30개, 스크립트 7개가 FUSEWILD가 아니라
"KX" 주식·회사 경영 시뮬레이터 소스였습니다 (`app/api/market/tick/route.ts`가
`kx_advance_market`을 호출). 특히 `SOURCE_OF_TRUTH_V1200.md`가 이 레포의 최신
기준 문서처럼 놓여 있어서 작업 방향을 오염시키고 있었습니다.
전부 `_archive/kx-project/`로 격리했습니다. **삭제하지 않았으니 확인 후 지우세요.**

그 외:
- 루트의 `app.js`(v5.4) · `styles.css` · `index.html` 등은 `public/`의 낡은 중복본
  → `_archive/stale-build/`
- 동일 내용 서비스워커 3개 → 1개
- `download`, `download (1)`, `index.js`, `game.js`, `assets/*/1` 제거
- 루트 문서 159개 → `docs/` · `docs/archive/`
- 버전 표기 통일: package.json · server.js · index.html · version.json 모두 6.3.0

## 테스트: 13/35 → 23/23

원본은 35개 중 22개가 실패 상태였습니다. 원인이 셋으로 갈렸습니다.

1. **버전 핀 (17개)** — `hz.version!=='5.5.0'` 처럼 자기 릴리스의 버전·deployId를
   하드코딩. 기능은 멀쩡한데 버전이 오를 때마다 하나씩 죽었습니다. 핀을 풀어
   "서버가 살아 있고 버전을 보고하는가"만 확인하도록 바꿨습니다.
2. **삭제된 API·문자열 (9개)** — v5.6에서 없앤 카드 전투 API나, 이후 제거·개명된
   CSS 클래스·UI 문구를 찾고 있었습니다. 되살릴 수 없어 `scripts/legacy/`로
   은퇴시켰습니다. 사유는 `scripts/legacy/README.md`에 표로 정리했습니다.
3. **경로 오류 (1개)** — `v531`이 루트 stale 사본을 읽고 있어 `public/`로 수정.

새로 추가한 테스트:
- `scripts/shop-cadence-test.js` — 매 웨이브 상점 개장, 구매 시 골드 차감,
  무료 보상 1개, 재고 차등을 검증
- `scripts/v63-trainer-heal-test.js` — 10웨이브 NPC 결투 2체 편성과 대사 존재,
  여행은 회복 / 던전은 미회복을 검증

`npm test`가 빌드 후 살아 있는 테스트 전체를 돌리고 요약을 출력합니다.

## 남은 권장 사항

- `_archive/` 확인 후 삭제
- 후반(30층 이후) 골드 과잉 조정
- 테스트에 버전 문자열·CSS 클래스명 하드코딩 금지 — 이번 22개 실패의 직접 원인

---

# 추가 수정 (같은 세션, v40 조사 중 발견)

## 버그: 융합 몬스터 이름의 절반 가까이가 "undefined"로 표시됨

`v40-monster-evolution-test`가 8번 중 3번꼴로 실패해서 원인을 추적했더니,
**실제 화면에 노출되는 표시 버그**였습니다. 융합 몬스터 이름 생성 시
코드네임을 고르는 인덱스 계산이 다음과 같았습니다:

```js
code = FUSION_CODENAMES[((h>>>8) ^ Math.imul(h, 31)) % FUSION_CODENAMES.length]
```

`Math.imul`은 **부호 있는** 32비트 정수를 반환합니다. XOR 결과가 음수가 되면,
자바스크립트의 `%`는 (수학적 모듈로와 달리) 부호를 유지하므로 배열에
**음수 인덱스**로 접근하게 되고, 존재하지 않는 인덱스라 조용히 `undefined`가
나옵니다. 10만 개 해시값으로 통계를 내보니 **46.9%** 확률로 재현됐습니다.
실제로 "홍련뇌전의 라이칸로드 · 오리진 undefined" 같은 이름이 화면에
뜨고 있었습니다.

**수정:** XOR 결과에 `>>>0`을 씌워 모듈로 전에 부호 없는 32비트로 강제
변환. 인덱스가 항상 0~15 범위에 들어옵니다.

```js
const codeSeed = (((h>>>8) ^ Math.imul(h, 31)) >>> 0) % FUSION_CODENAMES.length;
```

재발 방지로 두 가지를 추가했습니다:
- `scripts/fusion-name-fuzz-test.js` — 몬스터 204쌍 전체의 이름 생성 결과에
  "undefined"가 없는지 통계로 검증
- `v40-monster-evolution-test.js`에 융합 직후 이름 검사 1줄 추가

## 테스트 결함: v40의 "single-active switch" 검사가 동전 던지기였음

같은 조사에서 두 번째 원인도 나왔는데, 이건 게임 버그가 아니라 **테스트
설계 결함**이었습니다. v40 테스트는 균열개화(Rift Bloom) 검증을 위해
벤치 몬스터를 일부러 HP 50%로 만들어 두는데, 그 몬스터를 그대로 이어서
"교대" 검증에도 재사용했습니다.

싱글 액티브 모드에서는 교대가 행동 1회를 소모하고 그 자리에서 바로
적 턴이 이어집니다(포켓몬류의 표준 동작 — 교대하면 다음 타격을 그대로
맞습니다). 그래서 이미 50%인 몬스터가 교대 직후 다음 공격에 종종
쓰러졌고, 그러면 게임이 정상적으로 벤치의 다른 몬스터를 자동 투입하는데
그게 테스트가 기다리던 몬스터가 아니었을 뿐입니다. `switch-monster` 자체는
문제없이 동작하고 있었습니다.

**수정:** 교대 직전에 해당 벤치 몬스터를 완전 회복시켜서, 이 단계가
"교대 로직이 맞는가"만 검증하고 "우연히 안 죽었는가"를 검증하지 않도록
했습니다. 8회 연속 재실행으로 안정성 확인.

## 최종 테스트: 24/24

`fusion-name-fuzz-test`가 추가되어 23개에서 24개로 늘었고, 전부
5회 연속 재실행으로 안정성을 확인했습니다.
