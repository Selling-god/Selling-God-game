# 판매의 신 - Premium Gameplay Patch v6.0

## 적용 파일
이번 패치는 **수정이 필요한 파일만** 들어 있습니다.

- `app.js`
- `styles.css`
- `public/app.js`
- `public/styles.css`

기존 프로젝트에서 위 4개 파일을 같은 경로에 덮어쓰면 됩니다.

## Render / build 폴더를 쓰는 경우
이 프로젝트는 `public/`을 기준으로 `out`, `dist`, `build`, `site`, 루트 파일을 다시 만드는 구조입니다.

파일 교체 후 프로젝트 루트에서:

```bash
npm run build
```

을 한 번 실행하면 모든 배포 폴더가 새 버전으로 동기화됩니다.

## 이번 버전 핵심
- M&A 회사 목록에서 **내 지분 즉시 표시**, 가로 스크롤 의존 제거
- 상단 회사 메뉴가 2줄로 깨지지 않도록 라벨 정리
- 프로젝트 시작 전 **3단계 CEO 착수 미니게임**
- 결과: `완벽 성공 / 성공 / 보통 / 실패`
- 미니게임 결과가 **실제 프로젝트 집행 예산에 반영**
- 매 DAY **긴급 이사회 이벤트**와 다음 프로젝트 보정
- **오늘의 도전(Daily Challenge)** + 시즌 점수
- **CEO 시즌 등급(S+~D)**, 최고 순위, 연속 성공 기록
- 바로 위 경쟁사를 보여주는 **Rival Target**
- M&A/해외진출/언론/완벽 성공 등을 자동 기록하는 **Highlight Reel**
- 유튜브/방송 화면용 **Creator HUD**
- 영상 설명용 현재 런 정보 **클립보드 복사 버튼**
- 프로젝트/성공/실패/뉴스/경보용 **Web Audio 효과음**
- 국내 언론 / 해외 언론 분리
- 언론 비용은 현재 서버 실제 차감 구조와 맞춰 표시
- 모바일/태블릿 대응 레이아웃 보강

## 검증
- `node --check app.js` 통과
- `npm run build` 통과
- root/public/out/dist/build/site 모두 READY 확인
