# KX CORPORATE V11.0 - Steam Retail Candidate

## 핵심 목표
V11은 "실행된다"가 아니라 **구매자가 이해하고 신뢰할 수 있는 경영게임인가**를 기준으로 조정한 버전입니다.

## 1. 시작 화면/무한 로딩 구조 수정
- 첫 게임 셸을 네트워크 요청보다 먼저 표시합니다.
- 시장, 개인계정, 게임, 회사 핵심 데이터는 제한시간을 둔 병렬 요청으로 전환했습니다.
- CEO 라운지와 회사 확장 데이터는 첫 유효 화면을 막지 않습니다.
- 8초 시작 watchdog으로 어떤 선택적 서버가 느려도 로고 화면에 무기한 남지 않습니다.
- 회사 핵심 장부가 늦으면 마지막 정상 상태 또는 명확한 재연결 상태를 표시합니다.
- 시작 중 기업뉴스 팝업을 보류하고 메인 UI가 준비된 뒤 표시합니다.
- 오래 걸린 과거 응답이 최신 회사 상태를 덮지 못하도록 snapshot epoch 검사를 추가했습니다.

## 2. 경영 홈을 실제 의사결정 중심으로 정리
- CEO PULSE → 돈의 흐름 → 현재 경영 국면 → 중기 기준 → 이사회 안건 → 업무실 순서로 읽히게 정리했습니다.
- 이사회 안건 버튼은 넓은 메뉴가 아니라 실제 결정을 내릴 세부 화면으로 바로 이동합니다.
- 상위 메뉴를 왕복해도 마지막 세부 업무 화면을 유지합니다.
- 제품 장부가 아직 로딩 중이라는 이유만으로 기존 대기업을 "시장 진입" 단계로 잘못 표시하지 않습니다.

## 3. 관리회계 일관성 강화
- 회계 필드 이름이 서버 세대별로 달라도 매출/원가/운영비/영업이익/순이익을 하나의 reconciler에서 계산합니다.
- CEO 홈과 재무 화면이 같은 손익 숫자를 사용합니다.
- `매출 - 매출원가 - 운영비 = 영업이익 → 순이익` 연결이 화면 안에서 맞아야 합니다.
- 상세 운영비 합계와 서버 집계 운영비가 다르면 차액을 `기타·집계 운영비`로 명시합니다.
- 완전한 자산/부채/자본 원장이 없는 상태에서 `BALANCE SHEET`라고 과장하지 않고 `유동성·운전자본` 관리화면으로 수정했습니다.
- 외상매출금/재고/외상매입금은 운전자본 잔액으로 표시하고 현금흐름과 혼동하지 않습니다.
- 영업/투자/재무 현금흐름이 서버에서 확정되지 않았으면 0원이 아니라 `집계 대기`로 표시합니다.

## 4. 경영권/M&A 의미 오류 방지
- 외부 주주 전체 지분과 실제 적대적 인수 위험을 별도 지표로 유지합니다.
- 수동적 대주주를 적대적 인수자로 자동 분류하지 않습니다.
- 중복 주주는 합산하고 지분율은 0~100% 범위를 벗어나지 않습니다.
- 경영권 화면은 긴 회사명/금액/지분율이 붙지 않도록 카드·표 계층을 유지합니다.

## 5. 상용 온라인 상태 원칙
- 현금·지분·직원 등 결과값은 서버 확정 성공만 반영합니다.
- 네트워크 실패를 클라이언트 임시 성공으로 처리하지 않습니다.
- 빠른 중복 클릭으로 같은 거래가 두 번 수행되는 것을 차단합니다.
- 기술적인 Postgres/PostgREST/JWT 오류는 플레이어에게 그대로 노출하지 않습니다.

## 6. 모바일/화면 세부 조정
- 390px 폭 실렌더에서 가로 overflow 0을 확인했습니다.
- 사업 운영 세부 메뉴는 작은 화면에서 3열/2열 그리드로 바꿔 숨은 가로 스와이프가 필요하지 않습니다.
- 회사 가치 등 큰 금액의 `억 원` 단위가 어색하게 한 글자씩 분리되지 않도록 조정했습니다.
- 회사 업무실의 작은 설명 pill은 모바일에서 제거하여 제목과 충돌하지 않게 했습니다.

## 7. V11 자동 QA
`npm run release`가 다음을 연속 검사합니다.
- release/build hash 동기화
- native alert/confirm 제거
- game logic invariants
- 17 workspace render matrix
- startup contract
- route integrity
- player-facing error sanitization
- stale-response/double-submit/cache regression
- 회계 숫자 reconciliation

## SQL
V11만을 위해 새로 실행할 Supabase SQL은 없습니다.
