# RIFT DECK V5.1 - Combat UX / High-DPI Patch

## 핵심 수정
- 전투 후 `먼저 야생 몬스터를 봉인하거나 지나가 주세요`가 반복되며 진행이 막히던 문제 수정.
  - 봉인은 이제 실제 전투 중에 처리되며 승리 후 별도 봉인 게이트를 만들지 않습니다.
  - V50 세션에 남은 구형 `room.capture`도 다음 진행 시 자동 정리됩니다.
- 더블 전투에서 출전 몬스터가 모두 행동하기 전에 턴을 넘길 수 없도록 서버 규칙 수정.
- 첫 몬스터가 기술을 쓰면 아직 행동하지 않은 다음 몬스터가 자동 선택되고 기술 메뉴가 바로 열립니다.
- 전투 UI를 큰 4개 명령과 큰 2x2 기술 버튼 중심으로 단순화. 작은 정보 칩/중복 텍스트 대폭 제거.
- 전투 종료 보상을 `경험치 요약 -> 큰 보상 3개 -> 다음 전투` 흐름으로 축소.
- 현장 정비 3개 버튼은 평소 숨기고 `정비` 버튼을 눌렀을 때만 모달로 표시.
- 시작 RUN TRAIT 선택도 큰 3개 카드만 보이도록 단순화.
- 전투 몬스터 영역과 명령 패널을 완전히 분리해 하단 잘림/덮임 방지.
- 205 기본 몬스터 + 615 변형폼을 2x 고해상도 PNG(일반 512, 보스 768)로 변환.
- 인게임 렌더링은 자연스러운 고해상도 스케일링을 사용하고, 과도한 `image-rendering: pixelated` 확대를 제거.
- idle 애니메이션을 `steps()` 방식에서 부드러운 ease-in-out으로 변경.
- 기술명/피해량을 타격 위치에 크게 표시하고 피격 플래시/흔들림을 강화.
- Render Static Site 오배포 진단 문구 및 Web Service 배포 가이드 업데이트.

## 배포
게임은 `server.js`를 실행하는 Node Web Service입니다. `out/` 정적 사이트로 배포하면 API가 없으므로 플레이할 수 없습니다.

## 최종 보강 (V5.1.1 UX pass)
- 시작 원정 특성 선택 시 뒤에 조우 화면을 함께 표시하지 않고, 선택 화면 하나만 단독으로 표시합니다.
- 특성 선택 후 일반 단일 경로는 자동으로 다음 조우로 이어집니다.
- 모바일 명령 UI는 에너지/행동 정보를 가로 1줄로 축소하고, `기술 / 스펠 / 교대 / 봉인`을 화면 폭 전체로 확대했습니다.
- 모바일 기술명/설명 글자 크기를 상향하여 작은 4~5px 텍스트 의존을 줄였습니다.
- 구형 `E 턴 종료` 키보드 단축키를 제거했습니다.
- 보스 전투 봉인은 서버에서도 `로열 봉인구 + HP 25% 이하` 조건을 강제합니다.
- 긴 연출 누적으로 전투가 끊겨 보이던 문제를 줄이기 위해 클라이언트 상태 전환 대기 상한을 14초에서 2.6초로 축소했습니다.
