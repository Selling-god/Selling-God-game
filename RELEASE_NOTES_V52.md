# RIFT DECK V52 - Classic Battle UX

## 핵심 목표
기존 몬스터 자산은 그대로 유지하면서 전투 화면을 더 단순하고 읽기 쉬운 휴대용 몬스터 배틀 구조로 재편했습니다. 새 몬스터 이미지를 추가하지 않아도 되며, 기존 205종 + 변형 폼 자산을 그대로 사용합니다.

## 수정 내용
- `/api/room/:id/vote` 레이스 조건 수정
  - 전투로 이미 넘어간 뒤 늦게 도착한 vote 요청을 HTTP 400으로 처리하지 않고 안전하게 무시합니다.
  - 같은 노드에 대한 중복 vote도 idempotent 처리합니다.
- 일반 층 조우 화면 자동 진행
  - 단일 경로에서는 클라이언트가 120ms 후 자동으로 경로 진입을 요청합니다.
  - 별도 선택 화면을 오래 보여주지 않습니다.
  - 실제 바이옴 분기에서만 경로 선택 UI가 남습니다.
- 시작 보너스 화면 간소화
  - 최초 1회, 3개의 큰 버튼만 표시합니다.
  - 세부 설명을 한 줄 중심으로 줄였습니다.
- 전투 화면 V52
  - 전투장 비중 확대
  - 아군은 아래쪽, 적은 위쪽으로 시선 흐름 분리
  - HP 상태창은 스프라이트를 가리지 않도록 분리
  - 하단은 메시지 박스 + 2x2 명령 메뉴 구조
  - 기술 선택도 2x2 큰 버튼
  - RUN/LOG/정보는 상단 작은 보조 버튼으로 이동
- 이미지 표시 품질
  - 기존 512/768 PNG를 새로 교체하지 않습니다.
  - 큰 화면에서 과도하게 확대하지 않도록 최대 표시 크기를 제한했습니다.
  - `image-rendering:auto`와 자연스러운 브라우저 다운샘플링을 사용합니다.
  - 배경과 몬스터 사이의 그림자/지면광을 추가해 따로 노는 느낌을 줄였습니다.
- 모바일
  - 2x2 기본 명령을 유지하고 스프라이트 크기를 화면 폭에 맞춰 제한합니다.
  - 상태창이 몬스터를 가리지 않도록 별도 위치 규칙을 적용합니다.

## 호환성
- 몬스터 이미지 경로 변경 없음
- 기존 세이브 구조 변경 없음
- 기존 205종/615 변형폼 자산 변경 요구 없음
- V51 위에 패치 적용 가능
